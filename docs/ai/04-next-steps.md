# Next Steps

Current open work for the frontend CMS builder and public rendering:

1. Add tablet-specific responsive controls, not only mobile overrides, for the main marketing sections.
2. Extend `Content / Style / Advanced` field grouping so every block is organized consistently in the inspector.
3. Add richer typography controls:
   - heading weight
   - body width
   - letter spacing
   - button text size
4. Add per-side spacing controls for the main sections:
   - padding top/right/bottom/left
   - margin top/right/bottom/left
5. Add more marketing-oriented blocks and variants:
   - pricing/comparison
   - steps/process
   - team members
   - trust bar / badges
   - newsletter / lead form
6. Improve editor UX for nested layouts:
   - clearer section/cell selection
   - stronger visual hierarchy
   - better parity between editor preview and public render
7. Expand reusable presets/templates after the final block set stabilizes.
8. Normalize remaining hardcoded frontend styles so theme/background/text overrides apply more consistently.
9. Re-check embed mode after more blocks are added, to ensure links and visibility rules still behave correctly.

Completed recently:

1. Shared section appearance controls: background, image background, text color, spacing, border, radius, shadow, overlay.
2. Marketing blocks for stats, service cards, logo strip, testimonials and posts list.
3. Reusable section templates and full page templates in the editor.
4. Import/export for saved templates.
5. Initial `Content / Style / Advanced` inspector tabs.
6. Mobile responsive overrides for the main marketing sections.
